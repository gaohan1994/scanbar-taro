import Taro from '@tarojs/taro';
import { View } from '@tarojs/components';

type Props = {};

class CalendarHeader extends Taro.Component<Props> {
  render () {
    return (
      <View>
        CalendarHeader
      </View>
    );
  }
}

export default CalendarHeader;