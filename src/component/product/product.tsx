import Taro from '@tarojs/taro';
import { View } from '@tarojs/components';
import './product.less';

interface Props { }

class Product extends Taro.Component<Props> {
  render () {
    return (
      <View>
        asd
      </View>
    );
  }
}

export default Product;