import Taro from '@tarojs/taro';
import { AtIndexes } from 'taro-ui';
import { View } from '@tarojs/components';

interface Props { }

class ProductMain extends Taro.Component<Props> {

  render () {
    return (
      <View>
        asd
      </View>
    );
  }
}

export default ProductMain;